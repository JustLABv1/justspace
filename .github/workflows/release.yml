name: Release

on:
  push:
    tags:
      - "v*.*.*"
      - "v*.*.*-*"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  CHART_NAME: justspace

jobs:
  build:
    name: Build & Push (${{ matrix.service }})
    runs-on: ubuntu-24.04
    permissions:
      packages: write
      attestations: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: app
            context: .
            file: Dockerfile
            tag_prefix: ""
            latest_tag: latest
          - service: backend
            context: services/backend
            file: services/backend/Dockerfile
            tag_prefix: backend-
            latest_tag: backend
          - service: frontend
            context: services/frontend
            file: services/frontend/Dockerfile
            tag_prefix: frontend-
            latest_tag: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ matrix.latest_tag }},enable=${{ !contains(github.ref_name, '-') }}
            type=semver,pattern={{version}},prefix=${{ matrix.tag_prefix }}
            type=semver,pattern={{major}}.{{minor}},prefix=${{ matrix.tag_prefix }}
            type=semver,pattern={{major}},prefix=${{ matrix.tag_prefix }}
            type=sha,prefix=${{ matrix.tag_prefix }}sha-

      - name: Build and push
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: mode=max
          sbom: true
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  # chart:
  #   name: Package & Push Helm Chart
  #   runs-on: ubuntu-24.04
  #   permissions:
  #     packages: write
  #     contents: read

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

  #     - name: Set up Helm
  #       uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5

  #     - name: Prepare chart metadata
  #       id: chart_meta
  #       run: |
  #         VERSION="${GITHUB_REF_NAME#v}"
  #         OWNER=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')

  #         echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
  #         echo "registry_repo=oci://${{ env.REGISTRY }}/${OWNER}/charts" >> "$GITHUB_OUTPUT"

  #     - name: Log in to GitHub Container Registry for Helm
  #       run: |
  #         echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ${{ env.REGISTRY }} \
  #           --username "${{ github.actor }}" \
  #           --password-stdin

  #     - name: Package Helm chart
  #       run: |
  #         mkdir -p dist
  #         helm package charts/justspace \
  #           --destination dist \
  #           --version "${{ steps.chart_meta.outputs.version }}" \
  #           --app-version "${{ steps.chart_meta.outputs.version }}"

  #     - name: Push Helm chart
  #       run: |
  #         helm push "dist/${{ env.CHART_NAME }}-${{ steps.chart_meta.outputs.version }}.tgz" \
  #           "${{ steps.chart_meta.outputs.registry_repo }}"

  release:
    name: Create GitHub Release
    runs-on: ubuntu-24.04
    needs:
      - build
      # - chart
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Detect pre-release
        id: release_type
        run: |
          if echo "${{ github.ref_name }}" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+-.+$'; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
            echo "make_latest=false" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
            echo "make_latest=legacy" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate changelog
        id: changelog
        run: |
          CURRENT_TAG="${{ github.ref_name }}"
          VERSION="${CURRENT_TAG#v}"

          # Determine previous tag
          PREVIOUS_TAG=$(git tag --sort=-version:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sed -n '2p')

          if [ -z "$PREVIOUS_TAG" ]; then
            # First release: include all commits
            COMMITS=$(git log --pretty=format:"- %s (%h)" HEAD)
          else
            COMMITS=$(git log --pretty=format:"- %s (%h)" "${PREVIOUS_TAG}..HEAD")
          fi

          # Group commits by type
          FEATURES=$(echo "$COMMITS" | grep -E "^- feat" || true)
          FIXES=$(echo "$COMMITS" | grep -E "^- fix" || true)
          CHORES=$(echo "$COMMITS" | grep -E "^- (chore|refactor|docs|style|test|ci|perf|build)" || true)
          OTHERS=$(echo "$COMMITS" | grep -vE "^- (feat|fix|chore|refactor|docs|style|test|ci|perf|build)" || true)

          BODY=""

          if [ -n "$FEATURES" ]; then
            BODY="${BODY}### Features\n${FEATURES}\n\n"
          fi
          if [ -n "$FIXES" ]; then
            BODY="${BODY}### Bug Fixes\n${FIXES}\n\n"
          fi
          if [ -n "$CHORES" ]; then
            BODY="${BODY}### Chores\n${CHORES}\n\n"
          fi
          if [ -n "$OTHERS" ]; then
            BODY="${BODY}### Other Changes\n${OTHERS}\n\n"
          fi

          REGISTRY="${{ env.REGISTRY }}"
          IMAGE="${{ env.IMAGE_NAME }}"
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          BODY="${BODY}### Docker Images\n\`\`\`\ndocker pull ${REGISTRY}/${IMAGE}:${VERSION}\ndocker pull ${REGISTRY}/${IMAGE}:backend-${VERSION}\ndocker pull ${REGISTRY}/${IMAGE}:frontend-${VERSION}\n\`\`\`\n\n"

          # Multiline output for GitHub Actions
          {
            echo "body<<EOF"
            printf "%b" "$BODY"
            echo ""
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          if [ -z "$PREVIOUS_TAG" ]; then
            echo "previous_tag=" >> "$GITHUB_OUTPUT"
          else
            echo "previous_tag=${PREVIOUS_TAG}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3
        with:
          name: ${{ github.ref_name }}
          body: ${{ steps.changelog.outputs.body }}
          prerelease: ${{ steps.release_type.outputs.prerelease }}
          make_latest: ${{ steps.release_type.outputs.make_latest }}
          generate_release_notes: true
